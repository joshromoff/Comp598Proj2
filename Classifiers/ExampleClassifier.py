


#input 1 is the dictionary of training data
#input 2 is the dictionary of test data
#both inputs have format: key = index, value = (tokenized text, classification)
#the list of strings are tokens.
#output should be a function f with input(tokenized text), output class
def main(training,test):
    def simple(text):
        return 1
    return simple


if __name__ == "__main__":
    main(sys.argv[1],sys.argv[2])
